apiVersion: apps/v1
kind: Service
metadata:
  name: gerencia-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gerencia
  template:
    metadata:
      labels:
        app: gerencia
    spec:
      containers:
      - name: gerencia
        image: rdrigomachado/gerencia:latest
        ports:
        - containerPort: 80
